name: AI Eval Benchmarks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  ai-evals:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run AI eval benchmarks
        run: npm run test:ai-evals
